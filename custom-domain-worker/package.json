{
  "name": "custom-domain-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for AppsfieldAI custom domains: registers customer hostnames via Cloudflare for SaaS, stores mappings in KV, and reverse-proxies verified custom-domain traffic to the Base44 app.",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
